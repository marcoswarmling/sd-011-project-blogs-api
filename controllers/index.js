const userController = require('./User');
const loginController = require('./Login');
const categoryController = require('./Category');
const postController = require('./BlogPost');

module.exports = {
    userController,
    loginController,
    categoryController,
    postController,
};