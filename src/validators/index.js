const { userSchema, loginSchema } = require('./userSchema');
const { postSchema, updatePostSchema } = require('./postSchema');

module.exports = {
  userSchema,
  loginSchema,
  postSchema,
  updatePostSchema,
};
